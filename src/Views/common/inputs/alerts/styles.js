import styled from 'styled-components'
import { COLOR } from '../../constants'

const StyledError = styled.span`
  color: ${COLOR.red};
  display: block;
  font-size: 0.75rem;
  font-weight: normal;
  position: absolute;
`

export { StyledError }
